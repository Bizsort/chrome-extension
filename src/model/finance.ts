export namespace Points {
    export enum RewardType {
        User_New = 1,
        User_Referral = 2,
        MTURK_Company_Add = 3,
        MTURK_Company_Update = 4,
        MTURK_Company_Tag = 6
    }

    export enum LedgerStatus {
        Processed = 1,
        Pending = 2
    }
}